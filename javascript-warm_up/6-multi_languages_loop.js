#!/usr/bin/node
// Prints three lines by using an array of strings and a loop

const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const item of array) {
  console.log(item);
}
